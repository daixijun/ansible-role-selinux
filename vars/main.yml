---
__packages:
  RedHat:
    - libselinux-python
    - selinux-policy
  Debian:
    - python-selinux
    - selinux-policy-default
    - selinux-policy-mls
packages: "{{ __packages[ansible_os_family] | default(__packages[ansible_distribution]) }}"

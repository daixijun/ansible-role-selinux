---
__packages:
  Redhat:
    - libselinux-python
    - selinux-policy
  Debian:
    - python-selinux
  Ubuntu:
    - python-selinux
packages: "{{ __packages[ansible_os_family] | default(__packages[ansible_distribution]) }}"
